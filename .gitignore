# Wi-Fi configuration (contains sensitive credentials)
wifi_config.h

# Secrets (API URL and key)
secrets.h

# Environment variables for scripts
.env

# Build artifacts
*.build/

# Project-specific Arduino environment (ESP32 2.0.17)
# Note: arduino-cli.yaml is committed (project config), but .arduino15/ is not (build artifacts)
.arduino15/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Output files
output/

# Device logs (copied from SD card)
logs/
